{
  "name": "club-seville-international",
  "version": "1.0.0",
  "description": "International Club for Water Hobbyists",
  "author": "aes",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "heroku-postbuild": "npm run build",
    "test": "mocha --exit \"./{,!(node_modules)/**/}*.test.js\"",
    "test-watch": "nodemon --exec \"npm test\"",
    "deploy:test": "npm run setupenv:test",
    "presetupenv:test": "npm install",
    "setupenv:test": "cross-env-shell NODE_ENV=production PORT=3001 DATABASE_URL=postgres://aes:password@localhost/nextseville API_URL=http://localhost:3001/api API_URL_BROWSER=https://next.clubseville.icu/api PAYPAL_RETURN_URL=https://next.clubseville.icu/store/checkout/thanks PAYPAL_CANCEL_URL=https://next.clubseville.icu/store/checkout \"npm run build:test && npm run start:test\"",
    "prebuild:test": "psql -U aes -tc \"SELECT 1 FROM pg_database WHERE datname = 'nextseville'\" | grep -q 1 || psql -U aes -c \"CREATE DATABASE nextseville\" && npm test",
    "build:test": "npm run build",
    "prestart:test": "pm2 -s delete nextseville || :",
    "start:test": "pm2 start server/index.js --name nextseville",
    "deploy:prod": "npm run setupenv:prod",
    "presetupenv:prod": "npm install",
    "setupenv:prod": "cross-env-shell NODE_ENV=production PORT=4001 DATABASE_URL=postgres://aes:password@localhost/clubseville API_URL=http://localhost:4001/api API_URL_BROWSER=https://clubseville.icu/api PAYPAL_RETURN_URL=https://clubseville.icu/store/checkout/thanks PAYPAL_CANCEL_URL=https://clubseville.icu/store/checkout \"npm run build:prod && npm run start:prod\"",
    "prebuild:prod": "psql -U aes -tc \"SELECT 1 FROM pg_database WHERE datname = 'clubseville'\" | grep -q 1 || psql -U aes -c \"CREATE DATABASE clubseville\" && npm test",
    "build:prod": "npm run build",
    "prestart:prod": "pm2 -s delete clubseville || :",
    "start:prod": "pm2 start server/index.js --name clubseville"
  },
  "dependencies": {
    "@nuxtjs/auth": "^4.5.3",
    "@nuxtjs/axios": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "cross-env": "^5.2.0",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.4.0",
    "knex": "^0.16.2",
    "nodemailer": "^4.7.0",
    "nuxt": "^2.3.4",
    "paypal-rest-sdk": "^1.8.1",
    "pg": "^7.7.1",
    "uuid": "^3.3.2",
    "vuetify": "^1.3.15"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "chai": "^4.2.0",
    "eslint": "^5.10.0",
    "eslint-loader": "^2.0.0",
    "eslint-plugin-vue": "^4.0.0",
    "mocha": "^5.2.0",
    "node-sass": "^4.11.0",
    "nodemon": "^1.18.9",
    "sass-loader": "^7.1.0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2",
    "supertest": "^3.3.0"
  }
}
